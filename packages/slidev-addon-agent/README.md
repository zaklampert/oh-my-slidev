# Slidev Addon Agent

This package is the future home of the presenter-sidebar addon for the Slidev agent.

In the current implementation, the Hub backend hosts the reusable agent runtime in-process and exposes `/api/agent/*` endpoints that this addon can target.
